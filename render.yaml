services:
  - type: web
    name: trinetra-oracle
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: MODE
        value: naive
      - key: HORIZON_DAYS
        value: "3"
